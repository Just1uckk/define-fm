import styled from 'styled-components';

import { Title } from 'shared/components/title/title';

export const ModalHeader = styled(Title)`
	padding-top: 4.9rem;
	padding-left: 2.5rem;
	padding-right: 2.5rem;
	flex-grow: 0;
`;
