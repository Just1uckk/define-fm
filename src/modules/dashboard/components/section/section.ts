import { SectionHeader } from './section-header';
import { SectionBody, SectionWrapper } from './section-wrapper';

export const Section = {
	Wrapper: SectionWrapper,
	Header: SectionHeader,
	Body: SectionBody,
};
